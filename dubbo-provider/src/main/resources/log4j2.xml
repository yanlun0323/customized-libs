<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" packages="com.customized.log.core.log4j">
    <Appenders>
        <Console name="STDOUT" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [${ctx:_traceid}] [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!--文件会打印出所有信息，这个log每次运行程序会自动清空，由append属性决定，这个也挺有用的，适合临时测试用-->
        <File name="ALL" fileName="logs/dubbo-provider.log" append="false">
            <ThresholdFilter level="debug" onMatch="ACCEPT" onMismatch="ACCEPT"/>
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [${ctx:_traceid}] %-5level %class{36} %L %M - %msg%xEx%n"/>
        </File>

        <!-- ${sys:user.home}/这个会打印出所有的info及以下级别的信息，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <RollingFile name="INFO" fileName="logs/dubbo-provider-info.log"
                     filePattern="logs/$${date:yyyy-MM}/dubbo-provider-info-%d{yyyy-MM-dd}-%i.log">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="[%d{HH:mm:ss:SSS}] [${ctx:_traceid}] [%p] - %l - %m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
        </RollingFile>

        <RollingFile name="WARN" fileName="logs/dubbo-provider-warn.log"
                     filePattern="logs/$${date:yyyy-MM}/dubbo-provider-warn-%d{yyyy-MM-dd}-%i.log">
            <ThresholdFilter level="warn" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="[%d{HH:mm:ss:SSS}] [${ctx:_traceid}] [%p] - %l - %m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
            <!-- DefaultRolloverStrategy属性如不设置，则默认为最多同一文件夹下7个文件，这里设置了20 -->
            <DefaultRolloverStrategy max="20"/>
        </RollingFile>

        <RollingFile name="ERROR" fileName="logs/dubbo-provider-error.log"
                     filePattern="logs/$${date:yyyy-MM}/dubbo-provider-error-%d{yyyy-MM-dd}-%i.log">
            <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="[%d{HH:mm:ss:SSS}] [${ctx:_traceid}] [%p] - %l - %m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
        </RollingFile>

        <!--定义一个Rewrite-->
        <Rewrite name="Rewrite">
            <CustomizedRewritePolicy/>
            <!--引用输出模板-->
            <AppenderRef ref="STDOUT"/>
            <AppenderRef ref="ALL"/>
            <AppenderRef ref="INFO"/>
            <AppenderRef ref="WARN"/>
            <AppenderRef ref="ERROR"/>
        </Rewrite>

    </Appenders>

    <Loggers>
        <Root level="DEBUG">
            <AppenderRef ref="Rewrite"/>
        </Root>
    </Loggers>
</Configuration>